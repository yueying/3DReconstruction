# Dummy CMake file: do not remove this, so all subdirectories under fblib/apps/* can be 
# processed without special cases. 
# In fact, this directory only contains common headers & files for other projects, 
# but builds no target at all.
